class Solution {
public:
    bool stoneGame(vector<int>& piles) {
        // there will be atleast one way by which alice will win
        // that means we should return true
        // Because problem is asking is there any way that Alice can win
        return true;
    }
};
